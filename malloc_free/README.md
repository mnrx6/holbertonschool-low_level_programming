# malloc_free

This project introduces dynamic memory allocation in C using `malloc` and `free`. It focuses on allocating memory at runtime, checking for allocation failures, initializing allocated memory, and releasing it properly to avoid memory leaks.
